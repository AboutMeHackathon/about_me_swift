# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AboutMe' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
	pod 'Just'

  # Pods for AboutMe

  target 'AboutMeTests' do
    inherit! :search_paths
    # Pods for testing
	pod 'Just'
  end

end
